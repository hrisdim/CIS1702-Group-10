
function npc_speech(noun):

    if noun is not a valid NPC:
        return "invalid input"

    output dialogue
    return "end of dialogue"

function drop_item(noun):

    if noun is not a valid item in inventory:
        return "invalid input"

    drop item on room floor
    return "dropped item"

function
n save_and_quit(room):

    game_data["saved_room"] = room
    write game_data to "save_game.json"
    end program
